<UserControl x:Class="EnglishLearner.App.Views.WordLearningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:EnglishLearner.App.ViewModels">
    <UserControl.DataContext>
        <vm:WordLearningViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F0F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: 顶部统计 -->
        <Border Grid.Row="0" Background="White" Padding="20,14"
                BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="{Binding GroupProgressText}" FontSize="14"
                               Foreground="#666" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="已学" FontSize="14" Foreground="#666" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding LearnedCount}" FontSize="22" FontWeight="Bold"
                               Foreground="#4CAF50" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Row 1: 学习卡片 -->
        <Grid Grid.Row="1" Margin="20,16,20,8">
            <!-- 学习中 -->
            <Border Background="White" CornerRadius="12" Padding="24"
                    Visibility="{Binding IsGroupComplete, Converter={StaticResource InvertBool}}">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.12"/>
                </Border.Effect>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock FontSize="48" FontWeight="Bold" Foreground="#333"
                                   HorizontalAlignment="Center" Text="{Binding CurrentWord.Text}"/>
                        <Button Command="{Binding SpeakCommand}" Cursor="Hand"
                                VerticalAlignment="Center" Margin="12,0,0,4" ToolTip="点击发音">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Grid>
                                        <Ellipse x:Name="circle" Width="36" Height="36" Fill="#E3F2FD"/>
                                        <TextBlock Text="&#x1F50A;" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="circle" Property="Fill" Value="#BBDEFB"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                    <TextBlock FontSize="20" Foreground="#888" HorizontalAlignment="Center"
                               Margin="0,12,0,0" Text="{Binding CurrentWord.Phonetic}"/>
                    <Border Height="1" Background="#E0E0E0" Margin="0,20,0,20"/>
                    <TextBlock FontSize="28" FontWeight="Bold" Foreground="#1E88E5"
                               HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"
                               Text="{Binding CurrentWord.Meaning}"/>
                    <TextBlock FontSize="15" Foreground="#777" HorizontalAlignment="Center"
                               Margin="0,18,0,0" TextWrapping="Wrap" TextAlignment="Center"
                               FontStyle="Italic" Text="{Binding CurrentWord.Example}"/>
                </StackPanel>
            </Border>

            <!-- 本组完成 -->
            <Border Background="White" CornerRadius="12" Padding="24"
                    Visibility="{Binding IsGroupComplete, Converter={StaticResource BoolVis}}">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.12"/>
                </Border.Effect>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="本组学习完成！" FontSize="28" FontWeight="Bold"
                               Foreground="#4CAF50" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding ProgressText}" FontSize="15" Foreground="#888"
                               HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    <Button Content="继续下一组" Margin="0,24,0,0" Padding="32,12"
                            FontSize="16" FontWeight="SemiBold" Cursor="Hand"
                            Command="{Binding ContinueToNextGroupCommand}"
                            Background="#4CAF50" Foreground="White"
                            Style="{StaticResource RatingButton}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Row 2: 底部操作 -->
        <Grid Grid.Row="2" Margin="16,0,16,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- 进度指示器 -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding ProgressText}" FontSize="16" FontWeight="SemiBold"
                           Foreground="#555" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Column="1" Content="下一个" Padding="20,10"
                    FontSize="16" FontWeight="SemiBold" Cursor="Hand"
                    HorizontalAlignment="Right"
                    Command="{Binding NextWordCommand}"
                    Background="#2196F3" Foreground="White"
                    Style="{StaticResource RatingButton}"/>
        </Grid>
    </Grid>
</UserControl>
