<UserControl x:Class="EnglishLearner.App.Views.MasteredWordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:EnglishLearner.App.ViewModels">
    <UserControl.DataContext>
        <vm:MasteredWordsViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F0F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: 统计栏 -->
        <Border Grid.Row="0" Background="White" Padding="24,16"
                BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <StackPanel Margin="0,0,8,0">
                        <TextBlock Text="总单词" FontSize="13" Foreground="#999" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding TotalCount}" FontSize="28" FontWeight="Bold"
                                   Foreground="#333" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="24,0,8,0">
                        <TextBlock Text="已学" FontSize="13" Foreground="#999" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding LearnedCount}" FontSize="28" FontWeight="Bold"
                                   Foreground="#2196F3" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="24,0,0,0">
                        <TextBlock Text="已掌握" FontSize="13" Foreground="#999" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding MasteredCount}" FontSize="28" FontWeight="Bold"
                                   Foreground="#4CAF50" HorizontalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
                <Button Content="刷新" HorizontalAlignment="Right" Padding="20,8"
                        FontSize="14" Cursor="Hand" Command="{Binding RefreshCommand}"
                        Background="#E0E0E0" Foreground="#555"
                        Style="{StaticResource NavButton}"/>
            </Grid>
        </Border>

        <!-- Row 1: 总进度条 -->
        <Border Grid.Row="1" Background="White" Padding="24,12" Margin="0,4,0,0"
                BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel>
                <Grid>
                    <TextBlock Text="掌握进度" FontSize="14" Foreground="#666"/>
                    <TextBlock HorizontalAlignment="Right" FontSize="14" Foreground="#666">
                        <Run Text="{Binding MasteryPercent, StringFormat={}{0:P1}, Mode=OneWay}"/>
                    </TextBlock>
                </Grid>
                <ProgressBar Value="{Binding MasteredCount}" Maximum="{Binding TotalCount}"
                             Height="10" Margin="0,8,0,0"
                             Foreground="#4CAF50" Background="#E0E0E0"/>
            </StackPanel>
        </Border>

        <!-- Row 2: 单词列表 -->
        <ScrollViewer Grid.Row="2" Margin="12,6,12,12">
            <ItemsControl ItemsSource="{Binding AllProgress}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="10" Padding="18,14"
                                Margin="4,4">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.08"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Text}" FontSize="17" FontWeight="SemiBold"
                                                   Foreground="#333" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Phonetic}" FontSize="14"
                                                   Foreground="#999" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Meaning}" FontSize="14" Foreground="#666"
                                               Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                                    <Border CornerRadius="12" Padding="12,4" HorizontalAlignment="Right">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#F5F5F5"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsMastered}" Value="True">
                                                        <Setter Property="Background" Value="#E8F5E9"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsLearned}" Value="True">
                                                        <Setter Property="Background" Value="#E3F2FD"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="13" FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#999"/>
                                                    <Setter Property="Text" Value="{Binding QuizCorrectCount, StringFormat='{}{0}/10'}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMastered}" Value="True">
                                                            <Setter Property="Foreground" Value="#4CAF50"/>
                                                            <Setter Property="Text" Value="已完成"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsLearned}" Value="False">
                                                            <Setter Property="Foreground" Value="#BBB"/>
                                                            <Setter Property="Text" Value="未学习"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
